import React from 'react';

const TopBar: React.FC = () => {
  return (
    <div
      style={{
        height: 40,
        display: 'flex',
        alignItems: 'center',
        flexShrink: 0,
      }}
    >
      TopBar
    </div>
  );
};

export default TopBar;
